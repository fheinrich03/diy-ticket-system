// ─── Spalten ───────────────────────────────────────────────────────────────
const COL_NAME     = 1;
const COL_CODE     = 2;
const COL_ADMITTED = 3;
const COL_PAID     = 4;
const COL_EMAIL    = 5;
const COL_ADULTS   = 6;
const COL_KIDS     = 7;
const COL_TOTAL    = 8;
const COL_PAID_EUR = 9;
const COL_SENT     = 10;

// ─── Preise ────────────────────────────────────────────────────────────────
const PRICE_ADULT = 3;
const PRICE_KID   = 1;

// ─── Event ─────────────────────────────────────────────────────────────────
const EVENT_NAME     = "Eid al-Adha 2026";
const EVENT_DATE     = "Sonntag, 7. Juni 2026";
const PAYMENT_DEADLINE = "Freitag, 29. Mai 2026";

// ─── Zahlung ───────────────────────────────────────────────────────────────
const IBAN          = "DE17 2004 1111 0291 8191 00";
const ORG_NAME      = "al-Badr";

// ─── Farben (Sheet-Zeilen) ──────────────────────────────────────────────────
const COLOR_PAID_EXACT   = "#8efaab"; // grün — genau bezahlt
const COLOR_PAID_OVER    = "#b7e1cd"; // hellgrün — zu viel bezahlt
const COLOR_PAID_PARTIAL = "#fadc82"; // gelb — teilbezahlt